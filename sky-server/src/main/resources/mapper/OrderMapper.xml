<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.OrderMapper">
    <select id="queryConditionSearchOrders" resultType="com.sky.vo.OrderVO">
        select orders.*, order_detail.order_id from orders, order_detail
        <where>
            orders.id = order_detail.order_id
            <if test="beginTime != null and endTime != null">
                and orders.order_time between #{beginTime} and #{endTime}
            </if>
            <if test="number != null">
                and orders.number like concat('%', #{number}, '%')
            </if>
            <if test="phone != null">
                and orders.phone like concat('%', #{phone}, '%')
            </if>
            <if test="status != null">
                and orders.status = #{status}
            </if>
        </where>
    </select>
</mapper>